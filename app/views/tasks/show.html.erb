<h2>
  <%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<p><%= link_to'Edit a task', edit_path %></p>
<p ><%= link_to "Delete #{@task.title}",
task_path(@task),
method: :Delete,
data: { confirm: "you sure bitch?" } %></p>
<p><%= link_to "back to list ", tasks_path %></p>

